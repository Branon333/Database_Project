# CRUD Application – Farm Products & Orders (Node.js + Express + MongoDB)

## 🚀 Overview
This is a simple CRUD API built using Node.js, Express, and MongoDB.  
It supports two entities:

- **Products**
- **Orders**

The project demonstrates RESTful API design and database integration.

---

## 🛠️ Tech Stack
- Node.js
- Express.js
- MongoDB + Mongoose
- dotenv

---

## 📁 Project Setup

### 1. Clone the repository
```bash
git clone https://github.com/<your-username>/<your-repo>.git
cd crud-app
2. Install dependencies
npm install
3. Create a .env file
MONGO_URI=mongodb://localhost:27017/Database_Project/farm
PORT=5000
4. Install nodemon
npm install nodemon /for persistent.
5. Run the server
npm run dev

Server runs at:
http://localhost:5000
